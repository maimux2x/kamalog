<div class="hstack gap-3 mb-3" data-nested-form-target="item">
  <%= form.collection_select :clay_id, Clay.order(:created_at), :id, :name, include_blank: true, hide_label: true, required: true, wrapper: {class: false} %>
  <%= form.number_field :weight, hide_label: true, wrapper: {class: false}, step: 'any' %>
  <%= form.collection_radio_buttons :weight_unit, ClayUsage.weight_units_i18n, :first, :last, hide_label: true, wrapper: {class: false} %>

  <div>
    <button
      type="button"
      class="btn btn-outline-danger"
      data-nested-form-target="removeButton"
      data-action="nested-form#remove"
    >
      削除
    </button>
  </div>
</div>
