<%= render 'breadcrumb', items: [
  [current_studio.name, studio_path(current_studio)],
  ['設定', studio_settings_path(current_studio)],
  '教室'
] %>

<%= bootstrap_form_with model: @studio, url: [@studio] do |f| %>
  <%= f.text_field :name, label: '名前' %>

  <%= f.primary %>
<% end %>
